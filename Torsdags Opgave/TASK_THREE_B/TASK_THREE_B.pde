int min = 4; // Værdi for min
int max = 7; // Værdi for max

if ((min + max > 10) && (min <= 5 || max <= 5)) {
  //hvis ja
  println("Success!");
  //hvis nej
} else {
  println("Failure!"); // Hvis en af betingelserne ikke er opfyldt
}
